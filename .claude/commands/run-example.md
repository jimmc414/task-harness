# Run Example

Quick sanity check - run the example pipeline in dry-run mode.

```bash
python -m harness.cli run example --dry-run
```

Then show the pipeline details:

```bash
python -m harness.cli show example
```

Report whether the example pipeline is working correctly.
